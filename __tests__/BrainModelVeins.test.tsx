import '@testing-library/jest-dom';

describe('BrainModelVeins', () => {
  test('module exists', () => {
    // Placeholder — vein system is a visual-only shader component
    // rendered inside R3F Canvas; covered by integration/visual tests
    expect(true).toBe(true);
  });
});
